{
    "tasks": [
        {
            "type": "process",
            "label": "Build and run adaptodb",
            "command": "export PATH=\"$PATH:$(go env GOPATH)/bin\" && make build && ./adaptodb",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            // "problemMatcher": [
            //     "$gcc"
            // ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "isBackground": true,

        },
        {
            "type": "process",
            "label": "Build all",
            "command": "export PATH=\"$PATH:$(go env GOPATH)/bin\" && make build",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ],
    "version": "2.0.0"
}